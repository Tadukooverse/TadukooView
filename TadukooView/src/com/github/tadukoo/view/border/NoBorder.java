package com.github.tadukoo.view.border;

import javax.swing.border.AbstractBorder;

/**
 * No Border is an implementation of {@link AbstractBorder} that works as no Border. This is for use when you need
 * to specify a Border, but don't actually want one (i.e. null doesn't mean no border for some reason).
 *
 * @author Logan Ferree (Tadukoo)
 * @version Alpha v.0.3
 */
public class NoBorder extends AbstractBorder{
}
